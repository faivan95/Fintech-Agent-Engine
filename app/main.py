import time

if __name__ == "__main__":
    print("FinTech Agent Engine is running in the background...")
    # This loop keeps the Docker container permanently alive
    while True:
        time.sleep(60)